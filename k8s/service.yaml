apiVersion: v1
kind: Service

metadata:
  name: engagement-service
  namespace: revhive

spec:
  selector:
    app: engagement-service

  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085

  type: ClusterIP
